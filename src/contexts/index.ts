import { createContext } from "react";


export const usersContext = createContext(null);
export const SetUsersContext = createContext(null);
export const messagesContext = createContext(null);
export const setMessagesContext = createContext(null);
export const activeUserIdContext = createContext(null);
export const setActiveUserIdContext = createContext(null);